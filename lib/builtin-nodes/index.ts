/**
 * Built-in node definitions — Python sources + node metadata for nodes that
 * ship with the app (dragged from the Nodebar's built-in sections).
 */
export { GENOME_BROWSER_SOURCE, GENOME_BROWSER_NODE } from './genome-browser';
export { NGL_VIEWER_SOURCE, NGL_VIEWER_NODE } from './ngl-viewer';
